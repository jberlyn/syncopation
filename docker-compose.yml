version: '3.8'

services:
  joplin-server:
    build: .
    container_name: joplin-server
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8080
      - DB_PATH=/app/data/joplin.sqlite3
